<%= form_for(@favorite) do |f| %>
  <% if @favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% @favorite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row ">
    <div class="field large-7 large-centered columns">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
  </div><div class="row ">
  <div class="field large-7 large-centered columns">
    <%= f.label :from %><br />
    <%= f.text_field :from %>
  </div>
</div>
<div class="row">
  <div class="field large-7 large-centered columns">
    <%= f.label :to %><br />
    <%= f.text_field :to %>
  </div>
</div>
  <div class="actions large-2 large-centered columns">
    <%= f.submit  "Submit" %>
  </div>
<% end %>
